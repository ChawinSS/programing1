/*Programming Errors

Syntax error
These errors are mainly occurred due to the mistakes while typing 
These errors can be easily debugged or corrected.

(})
Displaying the value of a variable without its declaration. 
If we miss the semicolon (;) at the end of the statement.
*/

/*
Run-time error
 during the execution-time
 not able to perform the operation is the main cause of the run-time
erro
errors are very difficult to find
*/

/*
Linker error
the executable file of the program is not created
 the wrong function prototyping or usage of the wrong header file.
erro
most common linker error that occurs is that we use Main() instead of main()
*/

/*
Logical error
The logical error is an error that leads to an undesired output
produce the incorrect output, but they are error-free, known as logical errors.
*/

/*Semantic errors
Semantic errors are the errors that occurred when the statements are not understandable by the compiler
Ø Use of an un-initialized variable. 
Ø Type compatibility
Ø Errors in expressions
Ø Array index out of bound*/